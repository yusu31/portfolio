# 設計書: 街を貫く1本道とスポーツ施設（リビルド）

> 対象 Issue: #375 / 前提: `docs/HANDOFF_PHASE6_SESSION9.md`
> 位置づけ: **見た目は B / 骨格は Lempens・②** の方針を、実装できる粒度まで落とした設計書。
> この文書は**コードを1行も書かずに**作られている。数値はすべて「出発値」であり、
> §6 の再導出手順に従って実測で閉じる。

---

## §0 この設計書が引き受けた宿題

SESSION9 の HANDOFF が列挙した8項目に加えて、本セッションで確定した2つの判断と、
そこから生まれた新しい宿題を扱う。

### 本セッションで確定した判断（2026-08-07・ユーザー選択）

| 論点 | 決定 | 却下した案 |
|---|---|---|
| 「入っていく」の形 | **開ける型** — 街の建物列が途切れて視界が開き、道の横にコートが広がる。**屋根はかけない** | 通り抜け型（大屋根トンネル）／屋内型／章ごとに使い分け |
| 「出現」の見せ方 | **遠景で予告する** — 照明塔の頭が街の上にずっと前から見えている | 突然開ける／街が変形して立ち上がる |

### そこから生まれた宿題

「開ける型」は放っておくと、現行 `/scroll-poc` と同じ「**脇を掠めるだけ**」に落ちる。
屋根も門もくぐらないのだから、「入った」という身体感覚を**別の手段で**作らなければならない。
これを §1.2 で埋める。

---

## §1 世界の構造

### §1.1 基本のリズム — 圧 → 解放 → 圧

1章は4フェーズでできている。**街の圧が解けて、また閉じる**のがこの世界の呼吸になる。

| フェーズ | 長さ | 街の建物 | 何が起こるか |
|---|---|---|---|
| **街区** | 45 | あり（壁面 x=±6.0） | 建物が左右から画面を挟む。遠景に次の施設の照明塔が見えている |
| **導入** | 10 | **なし** | 敷地の入口。フェンスの切れ目、路面の切り替わり |
| **敷地** | 28 | **なし** | コートを並走。ボールの見せ場。奥にスタンドと照明塔 |
| **退出** | 9 | **なし** | フェンスが閉じ、街の建物が戻ってくる |

1章 = **92**。4章 = **368**。

この「圧 → 解放 → 圧」は、Naughty Dog の Emilia Schatz が言う空間による感情設計そのものである
（天井を下げ壁を寄せれば緊張、開ければ解放）。屋根が無くても、
**左右から挟む建物の有無**だけで同じことができる。

### §1.2 「入った」を身体に伝える4つの装置

屋根も門も無い以上、「入った」は次の4つの同時発火で作る。**1つでも欠けると脇を掠めるだけになる。**

#### 装置1: 街の壁が消える（圧の解放）

B では建物の壁面位置は定数だった。

```
FACADE_X = ROAD_HALF(4.2) + SIDEWALK_WIDTH(1.8) = 6.0   // src/proto/b/route.ts:36
```

これを「街区フェーズにだけ建物を置く」に変える。**重要なのは実装方法**で、
B の教訓（「区間ごとに街を生成すると境界で建物が食い合う」）を踏まないため、

> **生成は全域で連続に行い、敷地フェーズに落ちた建物をフィルタで抜く。**

生成のシード連鎖を切らないので境界が食い合わない。`buildStreet()` に
除外区間の配列を渡すだけで済む。

> ✅ **2026-08-08 実装済み（PR #388）。** `city/street.ts` の `buildStreet({ exclusions })`。
> テストで縛った言い換えが要点: **「除外区間を渡しても、街区に残る建物が1棟も動かない」**
> （「シード連鎖が切れていない」は抽象的すぎてテストにならないが、
> 除外あり／なしで生き残った建物を比べれば1行で書ける）。
>
> **境界を跨ぐ建物は落とさずに切り詰める**（幅を縮めて中心をずらす）ようにした。
> 落とすだけだと壁の終わりが建物1棟ぶん（最大13）手前まで下がって開口が締まらない。
> 切り詰めて 2.0 未満になったものだけ落とす。

「解放された」と読める条件は定量化できる。垂直画角 55° の半分は 27.5°、
16:9 の水平半画角は

```
atan( tan(27.5°) × 16/9 ) = atan(0.5206 × 1.7778) = 42.79°
→ 横位置 d の物体が画面端に来る前方距離 z = d / 0.9255
```

壁面 x=±6.0 の建物は、前方 6.5 ユニットで既に画面外へ抜ける。
つまり**近くの建物は常に画面外**で、圧を作っているのは**遠方の壁**である。
だから「開けた」と読ませるには、**視界深度ぶんの長さの開口が要る**。
B が `ROAD_RUNOUT = 95` を置いた根拠（「一点透視では常に70以上先が見えている」）から、

> **開口フェーズ（導入+敷地+退出）の合計 ≥ 45** を出発値とし、
> 実測で「街の壁が画面中央から消えている距離」を測って詰める。

出発値では 10 + 28 + 9 = **47**。

#### 装置2: 路面が変わる（足元で分かる）

B の `roadSurface.ts` は `STRIPS`（車道 → 縁石 → 歩道）を定数配列で持っている。
これを `t` 依存にする。

| 区間 | 車道 | 縁石 | 外側 |
|---|---|---|---|
| 街区 | アスファルト（`palette.road`） | あり（`CURB_HEIGHT = 0.16`） | 歩道（`palette.sidewalk`） |
| 敷地 | 敷地の舗装（章ごと: 土 / ハードコート / 砂） | **なし**（段差が消える = 敷地に入った合図） | コート面へ地続き |

**縁石が消えることが、路面の色より強い合図になる。** 段差は歩く体が覚えている情報だからだ。
`CURB_HEIGHT` を 0.16 → 0 へ導入フェーズの10ユニットで smootherstep する。

#### 装置3: 敷地境界（金網フェンス）

門はくぐらないが、境界は要る。**金網フェンスの切れ目**を入口にする。

- フェンスは敷地の外周に立ち、道と交わるところで途切れる
- 高さ 4.0。カメラ（路面 +2.6）より高いので、**敷地に入る直前、視界の左右をフェンスが締める**
- 既存の `netWind.ts`（風シェーダー）がそのまま使える。**風で揺れる金網**は移植資産のうち
  最もコストが低くリターンが大きい

フェンスの端部（切れ目の柱）は少し太くし、コーナーポストとして視覚的な「柱」を立てる。
**これが門の代わりになる。** 屋根が無いので「くぐる」ではなく「抜ける」。

#### 装置4: 見上げが変わる（上部構造の入れ替え）

B の `Overhead`（歩道橋・アーケード・電線）は「囲む構図」の本体で、
`maxOverheadGap()` がテストで上限を縛っている。敷地フェーズではこれを

- **照明塔**（高さ 26〜30。§1.3 のランドマークと同一の物）
- **防球ネット**（サッカー章のみ。上を横切る巨大な網）

に差し替える。街区では上が閉じ、敷地では上が抜ける（照明塔が点在するだけ）。
**上を見上げたときの情報量が入れ替わる**ことで、同じ屋外でも別の場所だと分かる。

> **4装置の同時発火**: 導入フェーズ 10 ユニットの中で、
> 縁石が消え、建物が途切れ、フェンスの柱を抜け、上部構造が照明塔へ替わる。
> **どれか1つだけを 10 ユニットかけて変えると鈍い。4つ同時に変えると「入った」になる。**

### §1.3 遠景予告 — ランドマークの配置（weenie）

Disney Imagineering の「weenie」（視覚的な磁石で人を前へ引く）を、そのまま採る。
スクロールに目的が生まれるので、Lempens の「区間ごとの動詞」に相当する役割も果たす。

**ランドマークが手前の建物に隠れないための条件を計算した。**

カメラが街区にいて、109 ユニット先の敷地を見ているとする。

```
手前 20 ユニットの建物（壁面 x=6.0）の水平角 = atan(6.0 / 20)  = 16.7°
                                上端の仰角 = atan((18-2.6)/20.9) = 36.4°  → 画面上端(27.5°)を超える
```

**つまり 20 先の建物は、画面中央から 16.7° の外側を上端まで塗りつぶしている。**
ランドマークがこれより外側にあると隠れる。

```
敷地中心 x=±29 に立てた場合の水平角 = atan(29/109) = 14.9°  → 隠れる（NG）
敷地の道寄り x=±12 に立てた場合    = atan(12/109) =  6.3°  → 隠れない（OK）
```

> **決定: ランドマーク（照明塔）は敷地の中心ではなく、道寄り（近サイドライン + 3 ≈ x=±12）に立てる。
> 高さは 26〜30。**（街の建物の最大高 18 に対し +8 以上）

高さの検算: 109 先・高さ 28 の塔の仰角は `atan(25.4/109) = 13.1°` で、画面の上半分に頭が出る。

**可視距離の目標: 110 以上手前から見えていること。** ただしフォグに負けてはならない。
B のパレットの `fogFar` は Morning 210 / Noon 220 / Amber 190 / **Night 160**。
夜が最も厳しいが 160 > 110 なので成立する。夜はむしろ**照明塔が点灯する**ので、
フォグの中の光点として最も強く見える。

| 章 | ランドマーク | 高さ |
|---|---|---|
| Projects（サッカー） | 照明塔 ×2 + 防球ネットの支柱 | 28 |
| Skills（バスケ） | 照明塔 ×2 + 大型スコアボード | 26 |
| About（バレー） | 照明塔 ×2 + 観客席スタンドの背面 | 26 |
| Contact | フィニッシュゲート（既存 `FINISH_GATE_*`） | 既存値 |

### §1.4 座標

道は -Z 方向へ進む。`t` は道に沿った距離。

```
ROAD_LEAD    = 16    （B と同じ。カメラが t=0 で後方にいるため）
ROAD_RUNOUT  = 95    （B と同じ。一点透視の奥を切らない）
TOTAL_LENGTH = 368   （4章 × 92）
```

| 章 | 街区 | 導入 | 敷地 | 退出 | 敷地中心 z |
|---|---|---|---|---|---|
| 1 Projects | 0 〜 -45 | -45 〜 -55 | -55 〜 -83 | -83 〜 -92 | **-69** |
| 2 Skills | -92 〜 -137 | -137 〜 -147 | -147 〜 -175 | -175 〜 -184 | **-161** |
| 3 About | -184 〜 -229 | -229 〜 -239 | -239 〜 -267 | -267 〜 -276 | **-253** |
| 4 Contact | -276 〜 -321 | -321 〜 -331 | プラザ -331 〜 -368 | — | **-349.5** |

**近サイドライン（道側の辺）は全章 x = ±9.0**（道の縁 6.0 + 敷地の余白 3.0）に揃える。
ヴェニュー中心はコート半幅ぶん外へ出す。左右は ② の慣習を引き継いで交互にする
（相対オフセットの符号を書き換えずに済むため）。

```
projects: (-29.25, 0, -69)     近サイドライン x = -29.25 + 20.25 = -9.0 ✓
skills  : ( 28.95, 0, -161)    近サイドライン x =  28.95 - 19.95 =  9.0 ✓
about   : (-28.95, 0, -253)    近サイドライン x = -28.95 + 19.95 = -9.0 ✓
contact : (  0,    0, -349.5)  道の正面（終着プラザ）
```

参考: ② では近サイドラインが x=0 だった（「カメラは引かないのでコートを3倍化し、
タッチライン際すれすれを並走させる」設計）。**今回はカメラが引ける**（§3）ので
その根拠は消え、球は道の上を離れない（§2.2）ので **9.0 外へ出す**。
これが HANDOFF の未解決事項「Q6c と Q8a の両立」に対する答えになる。

### §1.5 道の形

B の `elevationAt` / `lateralAt` をそのまま採る（下り坂 + 緩いうねり + 緩いカーブ）。
全長が 280 → 368 に伸びるので、周期パラメータだけ引き直す。

```
OVERALL_DROP     = 26 → 34   （u あたりの勾配を B と同じに保つ: 26 × 368/280 = 34.2）
UNDULATION       = 3.4       （振幅は据え置き。周期は u 基準なので自動で伸びる）
CURVE_AMPLITUDE  = 5.0       （据え置き。上げると消失点が画面中央から外れる = テストで縛る）
```

**下り坂であることには §7 で追加の役目がある**（落下の受け皿）。

---

## §2 章の割り当て

### §2.1 二層構造 — 街区が物語、敷地がセクション

B の `LEGS` は物語ラベル（体育館へ続く道 / 校庭沿い / 街の坂 / オフィス街）を持っていた。
② の `SECTION_RANGES` はポートフォリオのセクション（projects / skills / about / contact）を持っていた。
**この2つは競合しない。層が違う。**

> **街区フェーズ = 物語（体育教師 → エンジニア）。敷地フェーズ = セクション（Projects / Skills / About / Contact）。**

街を歩きながら来歴を語り、施設に着いたら作品を見せる。街の性格（建物の高さ・密度・パレット）が
章ごとに変わっていくことが、そのまま時間の経過になる。

| 章 | 街区の物語 | 敷地 = セクション | 動詞 | パレット | 建物高さ |
|---|---|---|---|---|---|
| 1 | 教えていた場所 | **Projects** / 屋外サッカーグラウンド | **蹴り込む** | Morning | [7, 12] |
| 2 | 走らせて、走っていた | **Skills** / 屋外バスケコート | **撃ち上げる** | Noon | [7, 14] |
| 3 | 独学の時間 | **About** / 屋外バレーコート | **受けて、上げる** | Amber | [9, 17] |
| 4 | いま作っている場所 | **Contact** / 終着プラザ | **着地する** | Night | [10, 18] |

パレットは B の4つ（Morning / Noon / Amber / Night）をそのまま採る。朝→夜へ空間的に移り変わる。

**未解決の既知バグ**: B の Amber 区間はオーバーレイ文字が読めない（橙の差し色を橙の空に重ねている）。
実装時に UI 文字色を `paletteAt(t)` から導出し、ΔE 閾値（§5.4）で縛る。

### §2.2 球は道を離れない — 施設は「的」であって「通り道」ではない

**これが今回の設計の要になる判断。**

「1本道を連続前進する」が必須要件である以上、球が道を外れてコートの中へ入ることはできない。
では見せ場はどこで起きるのか。

> **球は道の上を進み続ける。見せ場は「道の上から施設へ向けて」起こる。**

- Projects: 道を走りながらドリブル → **道の上からゴールへ蹴り込む** → ネットが揺れ、跳ね返って道へ戻る
- Skills: 戻ってきた球を道の上でキャッチ → **道からリングへ撃ち上げる** → リングを抜けて道へ落ちる
- About: 落ちてきた球を道の上でレシーブ → トス → **ネット越しにスパイク**
- Contact: 終着プラザへ着地して静止

この構造には3つの利点がある。

1. **1本道が守られる**（骨格の必須要件）
2. **既存の弾道ソルバー（`ball/physics/ballistic-trajectory.ts`）がそのまま生きる。** 距離が変わるだけ
3. **ネット物理（ベイク Verlet）が主役になる。** ② では通過するだけだったネットが、
   「撃ち込む的」になることで揺れが見せ場そのものになる

**一般則: 「見せ場の的」は敷地の道寄り3分の1に置く。コートの奥は絵（スタンド・照明塔）として使う。**

理由は §1.3 と同じ幾何にある。水平方向は画面幅で厳しく制限されるが（`d = 0.9255z`）、
**高さ方向は仰角で決まるので緩い**。

```
遠サイドライン x=48.9 の白線: 画面に入るには前方 53 必要 → 並走中は見えない
同じ位置の高さ 12 のスタンド : 前方 20 での仰角 atan(12/52.8) = 12.8° → 画面に入る
```

> **水平には画面外でも、垂直には入る。** だからコートの遠い辺は白線として見せず、
> **高さのある構造物（スタンド・フェンス・照明塔）で「場の終わり」を示す。**

### §2.3 的の位置（再導出が必要な箇所）

② の構造物オフセットは「近サイドラインが x=0」を前提にしていたので、そのままでは
的が道から遠すぎる。**的を道寄りへ寄せ直す**のが再導出の中身になる。

| 的 | ② の道からの水平距離 | 新しい目標 | 書き換える定数 |
|---|---|---|---|
| サッカーゴール前面 | 実質 6.5 | **12 前後** | `SOCCER_GOAL_GROUP_OFFSET.x` |
| バスケリング | 実質 5.1 | **11 前後** | `HOOP_GROUP_OFFSET.x` |
| バレーネット | 実質 0 | **10 前後** | `VOLLEY_NET_GROUP_OFFSET.x` |

「12 前後」の根拠: 水平距離 12 の的は、前方 13 ユニットで画面端に来る（`12/0.9255 = 13.0`）。
つまり**的が画面に入っているのは前方 13 以上の間**。敷地の長さ 28 に対して十分な滞在時間がある。

**`RING_OFFSET` は触らない**（HANDOFF の禁止事項）。動かすのは `HOOP_GROUP_OFFSET` の側。

---

## §3 カメラの文法

### §3.1 画角と、2つのカメラ姿勢

```
fov = 55   （B。「強い一点透視」の実体。A の 28 とは真逆）
```

B は主役が人（身長 1.8）、② は主役が球（半径 1.5）だったので、追従の4値が全く違う。

| | B（人物） | ②（球体） |
|---|---|---|
| dBack | 6.5 | 4.5 |
| dUp | 2.6 | 3.0 |
| lookAhead | **14** | **2** |
| lookUp | 2.2 | 1.5 |

**`lookAhead` の差が「一点透視の強さ」の正体である。** B が遠くを見ているから道が奥へ消える。
② は足元寄りなので、球は大きく写るが街の奥行きが出ない。

これは「見た目は B / 骨格は Lempens」の衝突点そのものなので、**計算で確かめた。**

### §3.2 検算 — B のカメラ値は球にそのまま使えない（2026-08-08 訂正・PR #382）

> ⚠️ **初版のこの節は、2つの行を別々の基準点で計算していた。**
> 街区（44% / screenY −0.287）は**路面基準**、見せ場（57% / screenY −0.726）は**球中心基準**。
> どちらの数値も単体では正しいが、**並べて比較できる表ではなかった。**
> §5.5 が守ると宣言している「カメラは `getBallFrame` の契約だけを消費する」に従うと
> anchor は**球中心**なので、B の路面基準の値は `BALL_RADIUS` を引いて移す必要がある。

基準点を球中心に統一して計算し直すと、次の3つが得られる（すべて実測で確認済み）。

```
共通: 球（半径 1.5）中心が anchor。カメラ位置 = anchor − heading×dBack + (0, dUp, 0)
      視線ターゲット = anchor + heading×lookAhead + (0, lookUp, 0)

(a) B の生値をそのまま入れる（dBack 6.5 / dUp 2.6 / lookAhead 14 / lookUp 2.2）
    screenY = −0.725 / 見かけ 44.0% / 上端 −0.285
    → **球が画面の下半分に沈み、全身がフレームに入らない。** 街区の構図として不成立

(b) BALL_RADIUS を引いて移す（dBack 6.5 / dUp 1.1 / lookAhead 14 / lookUp 0.7）
    screenY = −0.287 / 見かけ 44.2% / 上端 +0.155 / 下端 −0.729
    → 初版が「B の値」と書いていた構図はこれ

(c) 採用値（dBack 10.85 / dUp 2.0 / lookAhead 14 / lookUp 0.7）
    screenY = −0.251 / 見かけ 26.3% / 上端 +0.012 / 下端 −0.515
```

### なぜ (b) ではなく (c) を採ったか — 主役が人から球に変わった

B の主役は身長 1.8 の人で、**画面高の 26.5%** を占めていた。
球は直径 3.0 なので、**同じカメラ距離では 1.67 倍の 44.2% に写る**。
初版は「球は人より大きく写るが、主役が球だけなので存在感としてはこれで正しい」としたが、
**実機で撮り比べた結果、B と同じ 26.5% に揃える判断をした**（2026-08-08・ユーザー選択）。

> **決め手は見せ場との落差だった。**
> 見せ場は ② 値の 57% 固定なので、街区が 44% だと **44 → 57 の変化にしかならず、
> カメラの寄り引きがほぼ読めない**。26.5% にすると 2 倍以上に寄るので、
> 「街を見せる → ボールに寄る」という §3.3 の文法が絵として初めて立つ。

`dBack` を広げると副作用が1つ出る。**下り勾配 × dBack のぶんカメラの路面クリアランスが減る。**
B はカメラ高を「主役の**後ろ**の路面」基準にしてこれを避けていたが、
`getBallFrame` の契約に乗る以上その手は使えない（高さは球中心からの相対になる）。
実測で最小クリアランスが **0.6246** まで落ちたため、
**カメラが球の半径（1.5）より路面に近づかない**ことを条件に `dUp` を 2.0 へ引き直した（最小 1.5246）。

②の値（dBack 4.5 / dUp 3.0 / lookAhead 2 / lookUp 1.5）は元から球中心基準なので換算不要で、
`screenY = −0.726` / 見かけ 57%、**下側が 3 割ほど画面外に切れる**
（②のドキュメントが記録している「クロップ率平均 29%」と一致する）。

### §3.3 決定 — 街区は B 値、見せ場は ② 値

> **街区フェーズ = B 値（一点透視を強く、街を見せる）**
> **見せ場 = ② 値（球に寄る、ボールを追う）**

**すべて球中心（`getBallFrame` の anchor）基準。** 路面基準の値と混ぜない（§3.2）。

| 区間 | dBack | dUp | lookAhead | lookUp | 見かけの大きさ | 状態 |
|---|---|---|---|---|---|---|
| 街区・導入・退出 | **10.85** | **2.0** | 14 | **0.7** | **26.3%** | PR 1 で実測確定 |
| 敷地（見せ場） | 4.5 | 3.0 | 2 | 1.5 | 57.0% | PR 1 で実測確定 |
| 弾道の飛行中（arc） | 7 | 4.5 | 2 | 1.5 | — |
| 撃ち上げ（引き） | **17** | 3.8 | 2 | 1.5 | — |
| 落下（dive） | 1.5 | 7 | 0 | 0 | — |

**引きの機構は既に ② にある。** `freeThrowBlendT` が `D_BACK` を 4.5 → 17 へ smootherstep で
渡している（`src/journey/camera.ts:96`）。バックボードが「白い壁」になった問題を
**カメラを引くことで**解いた実績があり、同じ機構で「街区 ↔ 見せ場」も作れる。

### §3.4 機構の一般化 — blendT をテーブルにする

現在の `getCameraOffset(u)` は `arcBlendT` / `freeThrowBlendT` / `diveBlendT` の3つを
ハードコードで連鎖 lerp している。章が増えるたびに関数が増えるので、テーブルへ移す。

```ts
type CameraBeat = {
  from: number          // t（道に沿った距離）
  to: number
  rampIn: number        // 立ち上がりの長さ（ユニット）
  rampOut: number       // 立ち下がりの長さ（ユニット）。0 = 区間の終わりまで保持
  offset: Partial<CameraOffset>
}
```

> ⚠️ **2026-08-08 訂正（PR #382）。初版の `u` / `ease`（割合）から2点変えた。**

**1. 軸を `u` から `t` へ。** 見せ場は**世界の中の場所**であってスクロールの中の場所ではない。
PR 10 でワープ（`offset → t` の非線形写像）が入ると、`u` 基準のテーブルは敷地からズレる。

**2. ランプを割合ではなくユニットへ。** 割合は ② の2つの形を再現するための表現で、
/city の寄り引きは**フェーズ長（導入 10 / 退出 9）にぴったり合わせたい**。
割合のままだと章ごとのフェーズ構成の違いでズレる。実際、
**第4章は `exit` を持たないため、割合で書くと終着プラザの最後の 9.4 ユニットで
街区の構図へ戻っていた**（実装中に実測で発見）。ユニットなら `rampOut: 0` と書くだけで済む。

② の2つの形もそのまま表現できる。

- `diveBlendT`（三角形）= `rampIn + rampOut` が区間長と一致
- `arcBlendT`（台形・前後 12%）= `rampIn = rampOut = 0.12 × 区間長`

実装は**立ち上がりと立ち下がりの `min` を取る**だけで台形も三角形も出る。

- **区間が重ならないことをテストで縛る**（現行コードのコメントが前提にしている性質を、
  暗黙の了解からテストへ格上げする）
- **`rampIn + rampOut ≤ 区間長`もテストで縛る**（超えると台形の天井が 1.0 に届かない）

`applyCameraAttitude`（roll / pitch のキーフレーム）は**無変更で後段に残す**。既存契約のまま。

### §3.5 fov のパンチ（速度感）

fov は基本 55 固定だが、**落下とスパイクの瞬間だけ 55 → 68 へ吹かせて戻す**。
FOV を上げると画面端の物体が速く流れるため、速度の錯覚が最も安く手に入る手法である
（[Adding the Feeling of Speed](https://elliotdev.gg/adding-the-feeling-of-speed/)）。

- 立ち上がり 0.35 秒相当 / 戻り 0.8 秒相当を smootherstep
- **一点透視の強さが変わる**ので、見せ場の一瞬だけに限る。街区では絶対に触らない
- `prefers-reduced-motion` では振れ幅を 0 にする（既存 `useReducedMotion` に乗せる）

---

## §4 章の切替 — 二重実装をどう分けるか

「章内はグラデーション（B式）+ 章間はワープ（Lempens式）」をどう共存させるか。
**2つの実装が互いに直交するように軸を分ける。**

> **色は `t` の関数（1実装）。ワープは `offset → t` の写像（1実装）。**

### §4.1 色 — 位置ごとに焼く（B式・そのまま移植）

B の `paletteAt(t)` をそのまま採る。物の色はその物自身の位置で引く。

```
PALETTE_BLEND = 0.24   // 区間の終盤 24% だけ次のパレットへ渡す
```

**この 0.24 には根拠がある**（B の設計判断3）: 区間全部を使って渡すと、
中央が常に2色の 50/50 混色になり「パレットを絞る」が成立しなくなる。

色は `t` の連続関数なので、**ワープしても色は飛ばない**（`t` 自体は連続だから）。
急に変わるが、その瞬間はワープ VFX が画面を覆っている。

### §4.2 ワープ — スクロールと距離の写像に段差を入れる

```ts
distanceAt(offset): number
```

② では弧長ベースの線形写像だった。ここを**区分的に非線形**にする。

| 区間 | 1ページあたりの前進量 | 意図 |
|---|---|---|
| 通常 | 10.4（②/B と同じ） | 街を歩く速度 |
| **ワープ** | **42**（4倍速） | 25 ユニットを 0.6 ページで飛ぶ |
| **見せ場** | **6.5** | ボールの弾道を読ませるため減速 |

**ワープを撃つ位置**: 敷地を出て、**街が閉じきった後の街区**。

| ワープ | z 区間 | 長さ |
|---|---|---|
| 1（Projects → Skills） | -97 〜 -122 | 25 |
| 2（Skills → About） | -189 〜 -214 | 25 |
| 3（About → Contact） | -281 〜 -306 | 25 |

**開けた場所でワープしない理由**: 開けた場所は参照物が無いので「飛んだ」ことが分からない。
閉じた街区なら、前後で街の建物が入れ替わっても違和感が出ない。Lempens が
「章の切替をワープで隠す」と言うときの「隠す」はこの意味である。

移植する資産: `WarpFlash.tsx` / `warpVfxEnvelope.ts`（世界の形に依存しない）。

### §4.3 スクロール総量

```
実スクロール距離 = 368 − (3 × 25) = 293
PAGES = 293/10.4 + 3×0.6 ≈ 30
```

② は `PAGES = 27`、B も同水準。**世界は 368 に伸びたのに、スクロール量はほぼ変わらない。**
これがワープを入れる実利でもある。

---

## §5 移植リスト

### §5.1 新しいルート

| | 決定（2026-08-08 ユーザー確認済み） |
|---|---|
| URL | **`/city`** |
| ディレクトリ | **`src/city/`** |

`/scroll-poc` と並走させ、**超えたと確認できるまで ② を壊さない**。
`App.tsx` の `LegacyChrome` の除外リストに `/city` を追加する
（除外しないとローダーが画面を永久に覆う — B/C で踏んだ罠）。**PR 1 で対応済み。**

### §5.2 そのまま import する（世界の形に依存しない）

書き換えゼロ。新ルートから既存パスを参照するだけ。

> ⚠️ **2026-08-08 訂正（PR #382）: `CrystalBall.tsx` はこの表から外した。**
> `getBallPose`（②の経路）・`roll.ts`・`useScroll()` を直に読んでいるので、
> **そのまま import すると球は街の道ではなく②の軌道を走る。**
> `src/journey/` は現行シーンなので触れない（禁止事項）。
> → **材質のレシピだけを `city/CityBall.tsx` へ移植した。**
> 色・粗さ・透過・厚み・発光の数値は1つも変えていない（memory の「新規発明せず移植する」を守る）。
>
> **教訓**: 「世界の形に依存しない」かどうかは、**import 文を見るまで分からない。**

| ファイル | 何をする物か |
|---|---|
| `src/journey/nets/*` | ネットのベイク Verlet 一式。`ensureNetBake()` の契約は従来どおり |
| `src/journey/nets/netWind.ts` | 風シェーダー。**フェンスにも流用する**（§1.2 装置3） |
| `src/journey/WarpFlash.tsx` / `warpVfxEnvelope.ts` | ワープ VFX |
| `src/journey/DiveCloudVeil.tsx` / `diveVeilEnvelope.ts` | 雲ヴェール（§7 で使う） |
| `src/journey/ball/physics/ballistic-trajectory.ts` | 弾道ソルバー |
| `src/journey/useReducedMotion.ts` | — |
| `src/journey/skyConfig.ts` | 空 |

### §5.3 B から移す（見た目の本体）

| 元 | 先 | 書き換えるところ |
|---|---|---|
| `proto/b/route.ts` | `city/route.ts` | `LEGS` → 4フェーズ構造の章。`TOTAL_LENGTH` 280→368。`OVERALL_DROP` 26→34。`HERO_*` を削除（人物を入れない） |
| `proto/b/street.ts` | `city/street.ts` | `buildStreet()` に**除外区間**を渡せるようにする（§1.2 装置1）。`Overhead` を敷地では照明塔へ差し替え |
| `proto/b/boxes.ts` | `city/boxes.ts` | ほぼそのまま。`heroParts()` を削除 |
| `proto/b/roadSurface.ts` | `city/roadSurface.ts` | `STRIPS` と `CURB_HEIGHT` を `t` 依存へ（§1.2 装置2）。**遠景の地面（±200）を STRIPS に足す**（PR 2 で対応済み） |
| `proto/b/palette.ts` | `city/palette.ts` | そのまま + C の ΔE 規律をマージ（§5.4） |
| `proto/b/outline.ts` | `city/outline.ts` | そのまま。**採否は §9 で保留**（移植自体は PR 2 で完了 — `boxes.ts` が `OutlineBox` / `shouldOutline` に依存しているため） |

**捨てる**: `proto/b/Hero.tsx`（人物アバターは入れない — 主役は球体のみ）

> ⚠️ **2026-08-08 追記（PR #388）: B の `buildStrip` には巻き順のバグがある。**
> **負の横位置へ伸びる帯（道の左側）は三角形が裏向き**で、B は `side: DoubleSide` で
> 描いていたので消えずに出ていたが、**two-sided では three.js が裏面の法線を反転させる**ため
> 左側の帯だけ法線が下を向き、キーライトが当たっていなかった。
> 実測（`?leg=0&ph=open`、Morning、1280×800）で開口の地面が
> **左 `#3f5263` / 右 `#bdbcb5`** と左右で別物になっていた（修正後は両方 `#bdbcb5`）。
> B では建物が常に道の両側にあって歩道の外が見えなかったので露出しなかった。
>
> `city/roadSurface.ts` は `shouldFlipWinding()` で巻き順を揃え、`FrontSide` で描いている。
> **PR 3 でフェンス・縁石のランプを足すときも同じ規則に乗せること**
> （揃っていないと「暗くなる」ではなく「消える」ので気づける）。
> 縁石の法線も B の外向きから**内向き（= 道の上から実際に見える面）**へ直した。

### §5.4 C から移す（壁の情報量と、色の規律）

| 元 | 先 | 書き換えるところ |
|---|---|---|
| `proto/c/modules.ts` の `windowsOn` / `signsOn` | `city/facade.ts` | **座標系の変換が要る。** C の `Piece` は島タイルのローカル座標、B の `PlacedBox` は道の座標系（`roadPoint(t, offset)` 経由）。窓・看板の**貼り方のロジック**（グリッド生成・`PANEL_T = 0.06` の浮かせ・`WINDOW_ALT_RATIO = 0.3`）だけを持っていき、出力型を `PlacedBox` に合わせる |
| `proto/c/palette.ts` の `deltaE` / `hexToLab` / 7閾値 | `city/palette.ts` | **B の `Palette` 型はスロット構成が違う**（C は 9 スロット宣言、B は buildings 4 + accent + …）。閾値の対応表を作って移す |

**移す 7 閾値**（C で「壊れていた側の実測値」とセットで確定した値）:

```
MIN_STRUCTURE_DELTA_E        = 10   // 建物どうし（旧 Night 7.0 が「塊に溶ける」側）
MIN_GROUND_PATCH_DELTA_E     = 15   // 地面と地面パッチ（旧 Night 8.3 でパッチが消えた）
MIN_HERO_SKY_DELTA_E         = 45   // 主役の球と空
MIN_HERO_GROUND_DELTA_E      = 40   // 主役の球と地面
MIN_ROOF_SKY_DELTA_E         = 20   // 屋根と空（旧 Night 17.5 で「空に抜けた穴」に見えた）
MIN_ACCENT_STRUCTURE_DELTA_E = 18   // 差し色と建物
MIN_WINDOW_STRUCTURE_DELTA_E = 20   // 窓と建物
```

**捨てる**: 上部を横切る空中歩廊（C の俯瞰構図専用）/ 走路の蛇行（固定カメラ専用）/ 浮島 / カード / 固定カメラ

### §5.5 ② から移す（骨格）

| 元 | 先 | 書き換えるところ |
|---|---|---|
| `journey/camera.ts` | `city/camera.ts` | blendT をテーブル化（§3.4）。オフセット値は §3.3 の表へ |
| `journey/cameraAttitude.ts` | `city/cameraAttitude.ts` | キーフレームの `u` を新しい章割りへ引き直す。機構は無変更 |
| `journey/ball/beats/*` | `city/ball/beats/*` | **ビートの語彙（dribble / pass / receive / freeThrow / swish / fall / setToss / spike / rest）は全部残す。** 座標だけ引き直す |
| `journey/ball/anchors.ts` | `city/ball/anchors.ts` | **全面的に再導出**（§6）。ヴェニュー相対の比は保つ |
| `journey/ball/chase.ts` | `city/ball/chase.ts` | `getBallFrame(u)` の契約は不変。カメラはこの契約だけを消費する（設計原則3） |
| `journey/ball/roll.ts` | `city/ball/roll.ts` | **落下区間の回転則の分岐を足す（§7.3）。** テーブル生成ループの中だけで、テーブルの持ち方も呼び出し側も変更不要 |
| `journey/path/sections.ts` | `city/sections.ts` | 章の `u` 区間を新しい経路長から再導出 |
| `journey/venues.tsx` | `city/venues.tsx` | 構造物の描画。的の位置を §2.3 の表へ |

**捨てる**: `journey/DepthOutline.tsx`（現行シーン固有の制約に合わせた設計。B が既に却下している）

---

## §6 数値の再導出手順

**依存順に決める。** 上から下へ一方向で、逆流させない。

### Step 1 — カメラ定数を先に確定する ✅ **PR 1 で完了**

`fov = 55` と §3.3 の表を先に固定する。**これが以降すべての可視範囲を決める。**

測り方: `?leg=N&ph=P&at=M` で静止させ、`apparentBallFraction(t)` と `ballScreenY(t)` を実測。

**実測結果（`city/camera.test.ts` で固定済み）:**

| 量 | 実測値 | 縛り方 |
|---|---|---|
| 街区 見かけ / screenY | **26.34% / −0.2512** | B の主役（26.5%）に揃える |
| 見せ場 見かけ / screenY | **56.95% / −0.7257** | ② の値をそのまま |
| 寄りの比（見せ場 ÷ 街区） | **2.16 倍** | > 2 を必須にした（下回ると寄りが読めない） |
| `vanishingOffsetDeg` 最大 | **8.78°** @t=175.25 | B の上限 12° 以内 |
| カメラの路面クリアランス最小 | **1.5246** @t=124.25 | ≥ `BALL_RADIUS`（1.5） |
| 総回転 | **39.4 回転** | 経路長 ÷ 半径 を下回らない |

**壊れていた側の実測値も残す**（§9 の記録方式に合わせる）:

- `dBack 6.5`（B の生値）… 見かけ **44.2%**。見せ場 57% との比が **1.29 倍**しかなく寄りが読めない
- `dBack 10.85 / dUp 1.1` … クリアランス最小 **0.6246**。カメラが路面を擦る手前まで落ちる

### Step 2 — 水平半画角を実測する

```
16:9    → 42.78°  （d = 0.9255 z）
16:10   → 39.79°  （d = 0.8329 z）    ← 初版の「40.5° / 0.854」は誤り（2026-08-08 訂正）
4:3     → 34.76°  （d = 0.6941 z）
```

> ✅ **決定（2026-08-08・ユーザー選択）: サポートする最小アスペクト比は 16:10。
> 以降のすべての角度計算は `d = 0.8329 z` を基準にする。**
>
> 16:9 ではなく 16:10 にした理由は**計測環境と一致させるため**。
> QA スクリプト `scripts/qa-proto-shots.mjs` のビューポートが `1280×800`（= 1.6）で、
> proto/c も `QA_ASPECT = 1280/800` を画面内判定の基準に定数化している。
> 16:9 のままにすると**設計値と実測値が最初からズレる**。

これより縦長の画面は、垂直画角を広げて水平視野の欠けを部分補償する
（`city/route.ts` の `responsiveFov`。既存の `components/canvas/GlobalCanvas.tsx` と同じ
平方根で折半する Hor+ 方式を、基準だけ 16:10 に取り直したもの）。**テストでは縛らない。**

完全補償にしない理由: 4:3 で fov が 69.5 まで上がり、
**§3.5 が「落下とスパイクの一瞬だけ」に使うと決めた 68 を常用値が超えてしまう。**
一点透視の強さが常時変わるほうが、施設が少し切れることより害が大きい。
部分補償なら fov 59.4（水平半画角 34.76° → 37.2° まで回復）に収まる。

実測で確認済みの主要な距離（16:10 基準）:

```
的 x=12          → 前方 14.41 から画面内（敷地の長さ 28 に対し十分）
近サイドライン x=9.0 → 前方 10.81
街の壁 x=6.0     → 前方  7.20 で画面外へ抜ける
```

### Step 3 — 開口の長さを決める

「街の壁が画面中央から消えている」状態が何ユニット続くかを実測する。
目標: **45 以上**。出発値 47（10 + 28 + 9）。

測り方: 敷地フェーズの代表点で撮り、**画面中央の水平帯（y ∈ [−0.2, +0.2]）に
街の建物ピクセルが存在しないこと**を確認する。存在したら開口を伸ばす。

> ⚠️ **2026-08-08（PR #388）: これは PR 3 でしか測れない。**
> PR 2 で建物の除外は入り、**データ上の開口は章ごとに 47**（テストで固定済み。
> 左右それぞれ「壁が途切れている長さ ≥ 45」「壁の終わりが開口の入口から 1.0 以内」）。
> だが**上部構造（歩道橋）は敷地にも残っている**（差し替えは装置4 = PR 3）ので、
> 歩道橋の柱が画面中央の帯に入り、**建物ではないピクセルで判定が落ちる**。
> 装置4 と同時に測ること。

### Step 4 — コート寸法を決める

制約は1つだけ。**短辺（道に沿う z 方向）/ 球の見かけ直径 = 9〜10。**

② が Phase 5-5 で確立した比で、球が「コートの中の小さな主役」に見える値。

```
球の見かけ直径 = 3.0（icosahedronGeometry args=[1.5, 2]）
→ 短辺 = 27〜30 → 出発値 28.5（② の skills/about と同じ）
```

長辺（x 方向）は**画面に収まらないので制約しない**（§2.2）。② の値 39.9 / 40.5 を据え置く。

### Step 5 — 経路長と章の境界

```
章 = 街区 45 + 導入 10 + 敷地 28 + 退出 9 = 92
TOTAL_LENGTH = 368
```

敷地 28 は Step 4 のコート短辺 28.5 とほぼ一致させる（並走中ずっとコートの脇にいる）。

### Step 6 — VENUES 配置

§1.4 の表。**近サイドライン x = ±9.0 を固定し、中心はコート半幅ぶん外へ出す。**

### Step 7 — ランドマークの高さと横位置

§1.3 の計算をアスペクト比の実測値でやり直す。
判定: **章の 110 ユニット手前で、ランドマークの頭が画面内にあり、手前の建物に隠れていないこと。**

### Step 8 — 的の位置

§2.3 の表。「水平距離 12 前後 → 前方 13 以上の間ずっと画面内」を実測で確認する。

### Step 9 — ボールのビートアンカー

ヴェニュー相対オフセットの**比**を ② から保ちつつ、Step 8 の的の位置に合わせて引き直す。
`ballistic-trajectory.ts` に新しい距離を入れて、頂点高さが施設の構造物を越えることを確認する。

### Step 10 — クリアランス

② の `path.test.ts` が持っている「視線 AABB テスト」「構造物クリアランステスト」を移植し、
新しい座標で通ることを確認する。**カメラが構造物にめり込まない**ことがここで担保される。

---

## §7 落下演出 — 落下区間だけ回転則を差し替える

> ⚠️ **2026-08-07 訂正（Issue #377）。** 初版のこの章は
> 「球には向きが無いので回転で落下感を出せない」という前提の上に建っていたが、
> **これは誤りだった。**実コードを読めば分かることを、読まずに前セッションの HANDOFF から
> 引き継いでしまった。以下は実コードを確認して書き直したもの。

### §7.1 前提の訂正 — 球は無地でもないし、回転も既に入っている

| 誤っていた前提 | 実際 |
|---|---|
| 球は無地 | **320面の多面体 + `flatShading`**（`icosahedronGeometry args=[1.5, 2]`、`CrystalBall.tsx:50-52`）。コメントにも「サッカーボール風の細かい多角形面」とある。**面ごとに陰影が付くので回転が読める** |
| 球には向きが無い | 面の陰影に加えて、`transmission 0.7` / `ior 1.5` / `thickness 1.5` の**屈折像**と `clearcoat 1.0` / `roughness 0.01` の**ハイライト**が動く。向きの手がかりは3系統ある |
| 回転させる手段がない | **すでに実装・マージ済み**（PR #262）。`ball/roll.ts` の `getBallRollQuaternion(u)` が「滑らない転がり」を u の純関数で持つ（**全旅程で約27回転**）。`CrystalBall.tsx:39` で外殻に適用されている |

### §7.2 本当の穴 — 落下中だけ回転が止められている

```ts
// src/journey/ball/roll.ts:35-36
// 転がりを生むのは水平移動のみ。垂直バウンドで回転しない=ドリブルの物理として正しい
step.subVectors(cur, prev)
step.y = 0
```

**この1行が原因だった。** 水平成分だけが転がりを生むので、
**垂直優勢の落下軌道になるほど回転が止まる**。
（`fall.ts` は 2026-07-21 に垂直優勢へ軌道反転しているので、ここが最も効く区間になっている）

ドリブルのバウンドで球が回らないのは正しい物理だが、**同じ規則を落下に適用すると
「落ちているのに止まって見える」**という副作用が出る。

> **「落下感が出ない」の原因は「球に向きが無い」ではなく、
> 「落下中だけ回転が意図的に止められている」だった。**

原因が違うので、解も違う。

### §7.3 解 — Lempens の手法はそのまま使える

Lempens のスカイダイブの本体は「**カメラを回さず、アバター本人を宙返りさせる** + シェイク + パーティクル」
（`github.com/sebastien-lempens/webgl-skydiving`）。
**球にもこれがそのまま適用できる。**「アバターの宙返り」に相当するのが「球の回転則の差し替え」になる。

#### 落下区間の回転則

```
通常区間: 軸 = UP × 水平進行方向        角速度 = |水平変位| / BALL_RADIUS × ROLL_GAIN
落下区間: 軸 = カメラ右手方向（= 前方宙返りに見える軸）
          角速度 = |全変位（yを捨てない）| / BALL_RADIUS × FALL_ROLL_GAIN
```

- **軸をカメラ右手方向に取る**のが要点。ワールド固定軸だと、カメラとの相対で
  「横回転」に見える瞬間が出る。カメラ基準にすれば常に前方宙返りとして読める
- `FALL_ROLL_GAIN` の出発値は **1.6**（物理的に正確な 1.0 より速く回す。
  落下は「勢い余って回っている」ほうが読める）
- **両区間の境界は既存の blendT 機構で smootherstep 補間する**（§3.4 のテーブルと同じ形）。
  ここを切り替えると回転が飛ぶので、**角度ではなく角速度をブレンドする**

#### 実装は既存構造の拡張で済む

`roll.ts` は「モジュール初期化時に累積回転をテーブル化する」設計で、
`u が唯一の真実`・スクラブ逆再生で逆回転・リロードで再現、の3性質を満たしている。
**このテーブル生成ループの中で、u が落下区間に入ったら軸と角速度の式を差し替えるだけ。**
テーブルの持ち方も呼び出し側（`CrystalBall.tsx:38-39`）も変更不要。

> **`ROLL_GAIN` のコメントが既に警告している**: 「高速ビート（pass/spike）で
> ストロボ的に見える場合は QA で 0.6〜0.8 へ下げる」。
> 落下で回転を速める（1.6）ときは、**ストロボ化（回転が速すぎて逆回転や静止に見える）を
> 実機で必ず確認する**。フレームレート依存ではなく u 依存なので、
> スクロール速度が速いユーザーほど不利になる。

### §7.4 回転に足す層

回転が主装置に戻ったので、以下は**補助**になる。単独ではどれも弱いが、重ねると効く。

#### 層A: 世界が上へ流れる ★ 依然として最重要

回転は「球が動いている」ことは伝えるが、**「どれだけ速いか」の絶対的な尺度にはならない**
（等速で回る球と2倍速で回る球を見分けるのは難しい）。速度の尺度は
「脇を通過する物体がどれだけ速く流れるか」で読まれる。

> **落下は街区の上で起こす。開けた敷地の上では絶対に起こさない。**
> 敷地は参照物が無いので、どれだけ速く落ちても「浮いている」ように見える。

`§4.2` でワープを街区に置いたのと同じ理由であり、**落下 → ワープの順に並べれば
落下の終わりをワープで隠せる**（Lempens と同じ手順）。

参照物: 街の建物の壁（窓のグリッドがあるので流れが読める — §5.4 の窓が効く）、電柱、照明塔。
**落下経路のすぐ脇（水平距離 6〜10）に垂直材を通す。**

#### 層B: squash & stretch（回転の代替ではなく、回転に足す層）

初版では「回転の代替」としてこれを主装置に据えていたが、**回転が使えるので格下げする。**
ただし着地の潰れは依然として有効なので残す。

```
落下中    速度方向へ 1.00 → 1.20 / 直交へ 1.00 → 0.91    （初版の 1.35 より控えめに）
着地の瞬間 進行方向 0.75 / 直交 1.15 → 2〜3 バウンドで 1.0 へ
```

- **落下中の stretch を控えめにする理由**: 回転している球を強く引き伸ばすと、
  伸びた軸が回って**ラグビーボールが転がるように見える**。回転と stretch は干渉する
- **着地のスカッシュは強くてよい**。着地の瞬間は回転が止まるので干渉しない
- 実装は `CrystalBall.tsx` の**外側でグループの `scale` を操作する**だけ
  （球体本体のレシピには触らない — メモリの決定を守る）。
  **`quaternion` は `roll.ts` が持つので外側から触らない**（初版の記述を撤回）

#### 層C: 軌跡（トレイル）

球の後ろにリボンを引く。手法は `TrailRendererJS` 方式（局所空間の頭点列を
ワールド変換して既存の頭に繋ぎ、ジオメトリを伸ばす）を自前実装する
（[TrailRendererJS](https://github.com/mkkellogg/TrailRendererJS)）。

- 色は球の発光色（オレンジ）から `palette.accent` へ、末端で `sky` へ抜ける
- **長さを速度に比例させる。** 一定長だと「常に速い」ことになって速度差が読めなくなる
- 加算ブレンド + `depthWrite: false`

#### 層D: fov のパンチ

§3.5。55 → 68。**最も安く効く**（画面端の流れが直接速くなる）。

#### 層E: カメラシェイク

`applyCameraAttitude` の後段に足す。**振幅は必ず小さく。**

```
周波数 ≈ 18 Hz（Perlin ノイズで非周期に）
振幅   ≈ 0.02 rad（roll / pitch のみ。yaw は入れない = 進行方向がブレると酔う）
```

やりすぎると不快になる（「a little goes a long way」）ので、
**回転 + 層A〜D で足りない分だけを足す**という順序で調整する。
`prefers-reduced-motion` では 0。

#### 層F: 雲ヴェールと、地面の影

- `DiveCloudVeil`（既存資産）を落下の入口と出口に被せる。ワープと役割が近いので**同時に撃たない**
- **真下の影が近づいてくる**。高度の唯一の直接的な手がかりになる。
  落下経路の直下に地面があることを確認する（② には `groundHole` があり、
  Issue #344「地面の穴が通常再生では見えない」が未解決。新ルートでは穴を開けない前提で設計する）

### §7.5 どこで落ちるか

Skills（バスケ）でリングを抜けた球が、**次の街区の下り坂を落ちていく**。

B の道は `OVERALL_DROP` で下っている。**落下を道の勾配と結びつける**ことで、
「落ちた先にちゃんと道が続いている」が絵として成立する。

```
リングを抜ける（z ≈ -161 付近） → 街が閉じる（z = -184） → 落下（-184 〜 -189）
→ ワープ（-189 〜 -214） → About の街区へ
```

---

## §8 リッチにする工夫

> ユーザー指示: 「チープにならなければの話。しっかりリッチにできるよう工夫し、都度リサーチして」

投資先は**背景とオブジェクト**であり、クリスタル球ではない（球は現状で完成している）。

### §8.1 壁の情報量 — 窓と看板（最優先）

B の既知の弱点は「**建物が無地の板**。窓・看板が無く近景の壁が大きな単色面になる」だった。
C が既に解いているので移植する（§5.4）。

> ✅ **2026-08-08 追記（PR #388）: 窓とは別に「縦グラデーションの焼き込み」を先に入れた。**
> 壁が単色の板に見える原因のうち、**面そのものの明度が一定であること**に効く手当て。
> three 0.184 の `color_vertex.glsl` が `vColor.rgb *= color` → `*= instanceColor.rgb` と
> 掛け算する性質を使い、**共有ジオメトリに焼いた縦グラデ × インスタンスごとのパレット色**にした
> （箱が何百個あっても追加コストがゼロ / パレットの色数も増えない）。
>
> **出発値 0.78 → 1.10 では効かなかった。** 街区の壁は画面上端を越える高さなので
> **画面に写るのは箱のローカル高さの下半分だけ**で、底の値がそのまま可視範囲の暗さを決める。
> 実測: 0.78 で全可視高 **6%**（`#cfbbb9` → `#c3adab`。フォグの距離減衰と区別できない）→
> **0.60 で 11〜14%**（`#cebab8` → `#b8a09e`）。曲線は `curve 0.55`（足元に陰を集める）。

- **窓の点灯パターンを建物ごとに決定的乱数で焼く。** 毎フレーム更新しない
  （B の設計判断2「位置ごとに焼けば毎フレームのインスタンス色更新は不要で、絵としてもそちらが正しい」）
- 夜のパレットでは窓が唯一の明るい要素になる。`MIN_WINDOW_STRUCTURE_DELTA_E = 20` で縛る
- 日本の街なので**縦看板**を混ぜる。`palette.accent` のみ使用（パレット絞り込みを崩さない）

### §8.2 揺れる金網（既存資産の最大活用）

`netWind.ts` の風シェーダーを**敷地のフェンスと防球ネット**へ流用する。
ゴールネットのために書いた資産が、そのまま「街区にはない、敷地だけの質感」になる。

**巨大な網が上を横切る**（サッカー章の防球ネット）のは、
既存の Overhead（歩道橋・電線）より情報量が多く、しかも動く。

### §8.3 照明塔（ランドマーク兼、夜の光源）

- 昼: シルエットとして遠景予告（§1.3）
- 夜: **点灯する。** フォグの中の光点として最も遠くまで届く
- ゴッドレイは重いので使わない。代わりに**円錐メッシュに加算ブレンド + フレネル減衰**を焼く

### §8.4 遠景のスカイライン

施設の向こう（x が大きい側）に、低ポリのビル群をビルボードで置く。
**「街の中にある施設」であることが、施設の向こうにも街があることで初めて伝わる。**
`fogFar` に溶ける位置に置くので、ポリゴン予算はほぼ食わない。

### §8.5 夜の濡れた路面

スクリーンスペース反射は重い。代わりに**路面に空色の薄い鏡面ストライプを焼く**
（進行方向に伸びる細長い明度パッチ）。Night パレットでのみ有効にする。

### §8.6 雲

`drei` の `<Clouds>`。**`frustumCulled={false}` が必須**
（ワールド原点から離れた `<Cloud>` は既定 true では描画されない — 既知の罠）。

### §8.7 スクロールの緩急（§4.2 の再掲）

見せ場で減速し、ワープで加速する。**一定速度のスクロールは、それだけでチープに見える。**

---

## §9 輪郭線 — 決め方だけを決める（採否は保留）

B の `?ol=0` 撮り比べでは差が限定的だった。**「輪郭線が要る理由は壁に情報が無いから溶ける」
とも言えるので、窓を入れると要否が変わる。**

> **窓・看板（§8.1）を入れた後に測って決める。実装順序は §10 のとおり。**

### 測る量

「輪郭線が無いと溶ける」という主観語を、測れる量に翻訳する。

| 測る対象 | 量 | 判定 |
|---|---|---|
| 画面上で前後に重なる建物ペア | 両者の `deltaE` | ≥ `MIN_STRUCTURE_DELTA_E`(10) なら線なしでも分離する |
| 同ペアの境界コントラスト | 境界の左右 3px の平均輝度差 | 実測して閾値を決める |
| 建物と空の境界（屋根） | `deltaE` | ≥ `MIN_ROOF_SKY_DELTA_E`(20) |

### 判定手順

1. 窓・看板を入れる
2. 全4章 × 代表点3箇所を `?ol=0` / `?ol=1` で撮る（計 24 枚）
3. 各枚で上の3量を測る
4. **`?ol=0` で全閾値を満たすなら輪郭線は不要。** 満たさない章がある場合、
   その章だけ有効にするのではなく**全章で有効にする**（B の教訓「線は構図に従属する」より、
   構図が同じなら扱いも同じであるべき）

### 記録する値

**壊れていた側の実測値も必ず書き残す。**
C が `MIN_STRUCTURE_DELTA_E = 10` に対して「旧 Night 7.0 / 旧 Misty 7.8 が塊に溶ける側」と
記録していたから、後から閾値の妥当性を判断できた。同じ形式で残す。

---

## §10 実装順序 — 常に動く状態を保つ

**期限を切らない**方針なので、「大きな一発」ではなく「小さくマージし続ける」運用にする。
各 PR の終わりで `/city` が必ず表示できる状態を保つ。

| # | PR の内容 | 終わった時点で見えるもの | 依存 |
|---|---|---|---|
| ~~1~~ | ~~`/city` の骨格（道 + カメラ + パレット、街なし）~~ **完了 PR #382** | 何もない下り坂を球が進む | — |
| ~~2~~ | ~~街の生成（B の street / boxes / roadSurface / outline 移植）~~ **完了 PR #388** | B と同じ街を球が進み、**敷地では街の壁だけが途切れる** | 1 |
| 3 | **開口の残り**（縁石の消滅 + フェンス + 上部構造の入れ替え） | 街が開けて閉じる | 2 |
| 4 | 窓・看板（C 移植） | 壁に情報が入る | 2 |
| 5 | **輪郭線の判定**（§9） | 採否が決まる | 3, 4 |
| 6 | ランドマーク（照明塔）+ 遠景予告 | 目的地が見えている | 3 |
| 7 | 施設1（Projects / サッカー）+ 的の配置 | コートが1つ建つ | 3, 6 |
| 8 | ボールのビート1（dribble → kick → ネットが揺れる） | 見せ場が1つ動く | 7 |
| 9 | 施設2〜4 とビート2〜4（PR を分ける） | 4章そろう | 8 |
| 10 | ワープ（`distanceAt` の非線形化 + WarpFlash） | 章が繋がる | 9 |
| 11 | **落下の回転則差し替え（§7.3）+ 補助層 A〜F（§7.4）** | 落下が読める | 10 |
| 12 | Contact プラザ（終着・ジオラマ的ハブ） | 旅が終わる | 9 |
| 13 | リッチ化（§8.2〜8.6） | 質感が上がる | 12 |
| 14 | `/scroll-poc` との比較 QA | 超えたかどうかの判定 | 13 |

**PR 5 と PR 11 は判断を伴うので、単独で立てて計測結果を PR 本文に残す。**

`/scroll-poc` の差し替えは PR 14 で「超えた」と確認できてから、別 Issue で行う。

---

## §11 テストで縛るもの

A / B / ② で確立した「**仕様を目分量ではなく数値で固定する**」を継承する。
作り込みが粗い段階でも方向の定義だけは崩れないようにするため。

### 移植する縛り

| テスト | 縛る対象 | 出典 |
|---|---|---|
| `vanishingOffsetDeg(t) < 上限` | 一点透視の強さ（消失点が画面中央から外れない） | B |
| `enclosureElevationDeg(h) > 27.5°` | **街区で**建物が画面上端より上まで伸びる（囲む構図） | B |
| ↑ **B の式は移植できない**（2026-08-08 / PR #388） | B はカメラ高が路面基準の定数 2.6 だったが、/city は球中心基準で **1.52〜3.47 に散る**。B の式だと **36.60°** と実際より **4.6° 甘い**。街区の最も不利な地点で **31.98°**（@t=5.5）。**全長で測ると 24.44°（@t=239.75）で落ちるが、それは敷地＝建物が無い場所のカメラで測っているだけ** | — |
| `maxOverheadGap() < 上限` | 上部構造の隙間（空に抜けると囲む構図が壊れる） | B |
| ΔE 7閾値 | 色が溶けない | C |
| 視線 AABB / 構造物クリアランス | カメラが構造物にめり込まない | ② |
| `totalLegLength() === TOTAL_LENGTH` | 章の合計と経路長の一致 | B |
| 街の生成が境界で食い合わない | 建物の重なり検出 | B |

### 新しく足す縛り

| テスト | 縛る対象 | なぜ要るか |
|---|---|---|
| **`enclosureElevationDeg(h, facade) < 27.5°` が敷地で成立** | **敷地で開けている** | 「開ける型」の仕様そのもの。これが無いと開口が骨抜きになっても気づけない |
| **ランドマークの可視距離 ≥ 110、かつ手前の建物に隠れない** | 遠景予告 | §1.3 の角度計算をテストで固定する |
| **カメラの blendT 区間が重ならない** | §3.4 のテーブル化 | 現行コードがコメントで前提にしている性質を、テストへ格上げする |
| **球の `screenY` と見かけ大きさが目標範囲内** | §3.2 の構図 | 街区 44% / 見せ場 57% |
| **落下区間の脇に垂直参照物が存在する** | §7.4 層A | 「参照物が無いと速度が読めない」を仕様として固定する |
| **落下区間で回転が止まらない**（累積回転の u あたり増分が下限を超える） | §7.2 の再発防止 | `step.y = 0` と同じ罠を将来また踏まないため。**回転が止まっていたことに誰も気づかなかったのが今回の問題**なので、テストに気づかせる |
| **回転則の境界で角速度が連続**（隣接 u の角度差が上限以下） | §7.3 | 角度を切り替えると回転が飛ぶ。角速度をブレンドしていることを縛る |

### QA ノブ（最初から入れる）

A / B / C で確立した運用をそのまま採る。**検証したい違いはクエリで切り替えられるようにする。**

```
?leg=N        章を直接指定（スクロールを見なくなるのでカメラが静止し、2回目のポーリングで収束）
?at=0〜1      章の中の位置
?ph=street|open|venue|exit   フェーズ直接指定
?ol=0         輪郭線オフ（§9 の判定に必須）
?fall=0       落下演出の各層を個別にオフ（?fall=roll,trail,stretch,fov,shake で層を選択）
?rollGain=N   落下の回転ゲイン（既定 1.6）。ストロボ化の判定に使う
?warp=0       ワープ無効（経路を連続で確認する）
?land=0       ランドマーク非表示
```

スクリーンショットは既存の `scripts/qa-proto-shots.mjs <route> <outDir> <plansJson>`。
**Git Bash からは `MSYS_NO_PATHCONV=1` が必須。ヘッデッド必須。**
チェックポイントごとに単一 wheel + 安定するまでポーリング（この開発機で唯一収束する手順）。

---

## §12 却下した案とその理由

| 却下した案 | 理由 |
|---|---|
| **通り抜け型（両端の開いた大屋根）** | 私の推奨案だったが、ユーザーが「開ける型」を選択。屋根で空が切れると B の最大の武器（夕景1トーンを位置ごとに焼く）を捨てることになる、という批評家視点の指摘が採られた |
| **屋内型（建物の中で章が完結）** | 一点透視の消失点が壁で止まる。`ROAD_RUNOUT = 95` の前提（常に70以上先が見えている）が章のたびに崩れる。「1本道を連続前進」と構造的に矛盾する |
| **街が変形して立ち上がる** | B の設計判断2「パレットを位置ごとに焼く（世界は静的）」と衝突する。毎フレーム更新が復活し、B が捨てた設計へ逆戻りする |
| **章ごとに入り方を変える** | 各章に固有実装が要り工数が4倍化する。「1本道の旅」としてのリズムも崩れる |
| **球がコートの中へ入る** | 「1本道を連続前進」が守れない。施設を「的」にすることで、1本道・弾道ソルバー・ネット物理の3つが同時に生きる（§2.2） |
| **人物アバターを入れる** | 主役は球体のみと確定済み。ただし「球が主役として成立するのは C だけ」という SESSION7 の理由は誤りで、② が反証になっている |
| **C の空中歩廊** | C の俯瞰構図専用。B の一点透視では上部構造は歩道橋・電線で足りる |
| **C の走路の蛇行** | C の固定カメラ専用。連続追走では蛇行が消失点を振り、一点透視を壊す |
| **`journey/DepthOutline.tsx` の流用** | 現行シーン固有の制約（drei Cloud が depth を書かない）への対処。B が既に却下している |
| **街区でも B の追従4値をそのまま使う** | 主役が人（1.8）から球（直径3.0）に変わったので、同じ距離では 1.67 倍に写る。実機で撮り比べて B と同じ 26.5% に揃えた（§3.2）。見せ場 57% との落差が読めなくなるのが決定的だった |
| **カメラ高を「球の後ろの路面」基準にする（B の方式）** | 下り坂のクリアランスは確かに一定になるが、「カメラは `getBallFrame(u)` の契約だけを消費する」（設計原則3・§5.5）を破る。カメラが道を知ることになるので却下し、`dUp` を上げて解いた |
| **`CameraBeat` の `ease` を割合で持つ（初版 §3.4）** | 章ごとにフェーズ構成が違うとズレる。第4章は `exit` が無いため、終着プラザの最後の 9.4 ユニットで街区の構図へ戻っていた。ランプをユニットで持てば `rampOut: 0` で済む |
| ~~**回転で落下感を出す**~~ | **撤回（Issue #377）。** 「球体に向きが無いので成立しない」は誤りだった。球は320面の `flatShading` 多面体で、回転は `roll.ts` に実装済み（全旅程で約27回転）。真因は `roll.ts:36` の `step.y = 0` で落下中だけ回転が止まっていたこと。**回転は主装置として採用する**（§7.3） |
| **stretch を落下の主装置にする** | 回転が使えるので補助へ格下げ（§7.4 層B）。回転している球を強く引き伸ばすと、伸びた軸が回って**ラグビーボールが転がるように見える**（回転と stretch は干渉する） |
| **`CrystalBall` の外側から `quaternion` を操作する** | 初版の記述を撤回。`quaternion` は `roll.ts` が持っている。外から触ると二重管理になる |

---

## §13 未解決のまま残すもの

| 項目 | いつ決まるか |
|---|---|
| **輪郭線の採否** | PR 5（窓を入れてから測る）。§9 に手順を確定済み |
| ~~**ルート名 / ディレクトリ**~~ | **決定: `/city` + `src/city/`**（2026-08-08） |
| ~~**サポートする最小アスペクト比**~~ | **決定: 16:10 / `d = 0.8329 z`**（2026-08-08。§6 Step 2 参照） |
| **コート長辺の値** | §6 Step 4 で「制約しない」と決めたが、絵として不自然でないかは PR 7 の実機 QA で見る |
| **球と背景の ΔE が全章で閾値に届かない**（2026-08-08 / PR #388 で実測） | PR 4〜5。**主役の球が「その章の背景色」になっている。** `transmission 0.7` が背景を透かして写すのに `attenuationColor` が無いため。実測: Morning 街区で球↔空 **30.5**（閾値45）/ 球↔路面 **13.4**（閾値40）、Amber 街区で球↔壁 **4.6**（ほぼ同色）、Night 街区だけ球↔壁 **47.2** で成立。**Night で成立することが「材質が原因ではない」証拠**なのでクリスタルは変えない。手当ての順は `attenuationColor` + `attenuationDistance` → 発光をパレット明度に追従 → 球に暗いリム（街の箱は全部輪郭線を持つのに球だけ線が無い） |
| **Night 章の近景の壁がほぼ黒**（`#080407` を実測） | PR 4〜5。§5.4 の `MIN_STRUCTURE_DELTA_E = 10` で縛るときの「壊れていた側の値」 |
| **Amber 章の遠景の建物が黒い塊に見える** | PR 4〜5。`PALETTE_BLEND = 0.24`（22ユニット）に対し一点透視では 70 以上先が見えているので、遠景はもう完全に次章（Night）の色。**「進む先に夜が見える」効果自体は良い**が、渡しの距離が視界深度より短いので段差に読める |
| **Amber 章のオーバーレイ文字色** | B の既知バグ。§2.1 に記録。実装時に ΔE で縛る |
| **Issue #344（地面の穴）** | 新ルートでは穴を開けない前提。② 側の課題として残る |
| **`FALL_ROLL_GAIN` の値**（出発値 1.6） | PR 11 の実機 QA。**ストロボ化**（回転が速すぎて逆回転や静止に見える）を必ず確認する。u 依存なのでスクロールが速いユーザーほど不利 |
| **`?fall` の各層の最終的な配合** | PR 11 の実機 QA。回転 + 層A〜D で足りない分だけ層E を足す順序で調整する |

---

## 出典

- [Adding the Feeling of Speed — Elliot Couvignou](https://elliotdev.gg/adding-the-feeling-of-speed/)（FOV・速度感）
- [Amplify Your Game Camera — CG Cookie](https://cgcookie.com/posts/creating-a-great-game-camera)（カメラシェイク・FOV 連動）
- [TrailRendererJS — mkkellogg](https://github.com/mkkellogg/TrailRendererJS)（軌跡の実装方式）
- [High-speed Light Trails in Three.js — Codrops](https://tympanus.net/codrops/2019/11/13/high-speed-light-trails-in-three-js/)（発光トレイル）
- [Wayfinding in Themed Design: The "Weenie"](http://theoryofthemeparks.blogspot.com/2015/08/wayfinding-in-themed-design-weenie.html)（遠景予告＝視覚的な磁石）
- [Disneyland — The Level Design Book](https://book.leveldesignbook.com/studies/irl/disneyland)（長い見通し線とランドマーク）
- [Environmental Storytelling in Video Games — GameDesignSkills](https://gamedesignskills.com/game-design/environmental-storytelling/)（圧縮と解放による感情設計 / Emilia Schatz）
- `github.com/sebastien-lempens/webgl-skydiving`（Lempens のスカイダイブの実装。SESSION9 で確認済み）
